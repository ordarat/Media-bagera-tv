name: LiveZon Cloud Playout

on:
  workflow_dispatch:
    inputs:
      stream_url:
        description: 'لینکی ڤیدیۆ یان m3u8 لێرە دابنێ'
        required: true
        type: string
        default: 'http://185.191.126.127:8080/live///b0:99:d7:15:88:50/3090914536649669/358220.ts'
      logo_url:
        description: 'لینکی وێنەی لۆگۆکە'
        required: true
        type: string
        default: 'https://i.postimg.cc/Dwwpt9B0/1000034754-removebg-preview.png'
      ticker_text:
        description: 'نووسینی شریتی هەواڵەکان'
        required: true
        type: string
        default: 'بەخێربێن بۆ کەناڵی ناوخۆیی میدیا باگێرا و سیستەمی لایڤزۆن'

jobs:
  start_stream:
    runs-on: ubuntu-latest
    steps:
      - name: دابەزاندنی بەرنامەکان و فۆنتی دروست
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg python3-pip
          pip3 install Pillow arabic-reshaper python-bidi --break-system-packages
          wget -qO myfont.ttf "https://raw.githubusercontent.com/rastikerdar/vazirmatn/master/fonts/ttf/Vazirmatn-Bold.ttf"

      - name: دروستکردنی وێنەی شەفاف بۆ نووسینەکان
        env:
          TICKER_TEXT: ${{ github.event.inputs.ticker_text }}
        run: |
          cat << 'EOF' > format_text.py
          import os
          from PIL import Image, ImageDraw, ImageFont
          
          ticker = os.environ.get('TICKER_TEXT', 'بەخێربێن')
          static = "دەنگ و باس"
          
          def create_img(text, filename, font_size):
              font = ImageFont.truetype("myfont.ttf", font_size)
              try:
                  bbox = font.getbbox(text, direction="rtl", language="ckb")
                  width = bbox[2] - bbox[0]
                  img = Image.new("RGBA", (max(10, width + 50), 80), (0,0,0,0))
                  draw = ImageDraw.Draw(img)
                  draw.text((27, 12), text, font=font, fill=(0,0,0,255), direction="rtl", language="ckb")
                  draw.text((25, 10), text, font=font, fill=(255,255,255,255), direction="rtl", language="ckb")
              except Exception:
                  import arabic_reshaper
                  from bidi.algorithm import get_display
                  reshaper = arabic_reshaper.ArabicReshaper(configuration={'language': 'Kurdish', 'delete_harakat': True})
                  bidi_text = get_display(reshaper.reshape(text))
                  bbox = font.getbbox(bidi_text)
                  width = bbox[2] - bbox[0]
                  img = Image.new("RGBA", (max(10, width + 50), 80), (0,0,0,0))
                  draw = ImageDraw.Draw(img)
                  draw.text((27, 12), bidi_text, font=font, fill=(0,0,0,255))
                  draw.text((25, 10), bidi_text, font=font, fill=(255,255,255,255))
                  
              img.save(filename)

          create_img(ticker, "ticker.png", 42)
          create_img(static, "static.png", 40)
          EOF
          python3 format_text.py

      - name: دەستپێکردنی پەخش بە سیستەمی کاتژمێرەوە
        env:
          RTMP_URL: ${{ secrets.OKRU_RTMP_URL }}
          TZ: Asia/Baghdad
        run: |
          ffmpeg -re -i "${{ github.event.inputs.stream_url }}" -i "${{ github.event.inputs.logo_url }}" -i ticker.png -i static.png \
          -filter_complex "[1:v]scale=220:-1[logo];[0:v][logo]overlay=W-w-35:35[v1];[v1]drawtext=text='Media bagera tv':fontfile=myfont.ttf:fontcolor=white:fontsize=26:x=W-tw-35:y=270:borderw=2:bordercolor=black[v2];[v2]drawbox=y=ih-80:color=black@0.85:width=iw:height=80:t=fill[v3];[v3][2:v]overlay=x='W-mod(t*150\,W+w)':y=H-80[v4];[v4]drawbox=x=W-280:y=ih-80:color=#8B5A2B@1.0:width=280:height=80:t=fill[v5];[v5][3:v]overlay=x=W-280+(280-w)/2:y=H-80[v6];[v6]drawbox=x=0:y=ih-80:color=#8B5A2B@1.0:width=160:height=80:t=fill[v7];[v7]drawtext=fontfile=myfont.ttf:text='%{localtime:%R}':fontcolor=white:fontsize=45:x=(160-tw)/2:y=h-62:shadowcolor=black:shadowx=2:shadowy=2" \
          -c:v libx264 -preset veryfast -b:v 2500k -maxrate 2500k -bufsize 5000k \
          -c:a aac -b:a 128k -f flv "$RTMP_URL"
